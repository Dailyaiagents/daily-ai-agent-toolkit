# Contributing

Open an issue before a large change. Pull requests must include tests, update user-facing limitations, and avoid adding network calls or telemetry without an explicit design discussion.

Run `bash scripts/test-all.sh` before submitting a change.
